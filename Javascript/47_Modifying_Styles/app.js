const h1 = document.querySelector("h1");
console.log(h1.style); //inline styles

h1.style.color = "teal"; //setting inline style
h1.style.backgroundColor = "orange";

/* Note: Avoid setting inline styles like above instead use classes */
