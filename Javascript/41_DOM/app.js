// DOM means Document Object Model
// It represents the structure of a web document as a tree of objects

console.dir(document); // Displays the document object in the console
console.log(document.baseURI); // Logs the base URI of the document
console.log(document.location); // Logs the location object of the document
console.log(location.host); // Logs the host of the current URL
